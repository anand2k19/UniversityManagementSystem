import 'package:flutter/material.dart';

class SubjectTests extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Welcome to tests"),
    );
  }
}
